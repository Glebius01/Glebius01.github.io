title: "Hlib | Cyber Security Portfolio"
description: "Security labs, investigations, and technical write-ups"
markdown: kramdown

theme: null

plugins:
  - jekyll-feed

collections:
  labs:
    output: true
    permalink: /labs/:name/

defaults:
  - scope:
      path: ""
    values:
      layout: default
